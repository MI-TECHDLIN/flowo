import 'package:flutter/material.dart';

class DailyFlowScreen extends StatelessWidget {
  const DailyFlowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('daily Flow')));
  }
}
